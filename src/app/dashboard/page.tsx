import Layout from "@/components/Layout";
import { FC } from "react";

const HomePage: FC = () => {
  return (
    <Layout>
      <h1>Welcome back.</h1>
    </Layout>
  );
};

export default HomePage;
